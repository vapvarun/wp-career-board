---
id: <kebab-case-id>
priority: critical | high | medium
personas: <user_login_a>, <user_login_b>
requires: mu:autologin, seed:jobs, pro:<module>
last_verified: 2026-05-09
bug_ref: <Basecamp # or GitHub issue, optional>
---

# <Journey title — verb-first, customer-language>

**Why this journey exists:** <one sentence: what bug it guards / what contract it verifies>

## Steps

1. As `<persona>`, navigate to `<url>` → expect HTTP 200
2. <Action> → expect <visible state>
3. <REST/DB call> → expect <response shape>
4. <Side-effect check> → expect <persisted state>
5. tail debug.log diff → expect ZERO new fatal/warning lines

## Teardown

```bash
# Optional cleanup commands. Run unconditionally — must be safe to re-run.
wp post delete --force <id-pattern>
```

## Notes

<Any context the journey author wants future-you to know — selector quirks, environment caveats, acceptable variance.>
